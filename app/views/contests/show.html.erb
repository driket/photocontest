<h1><%= @contest.title %></h1>

<h3>
	<%= status_label(@contest.status) %>
	<span class="discreet"><%= contest_status_sentence(@contest) %></span>
</h3>

<p><%= @contest.body %></p>

<% if @contest.status == "open" %>

<%= render :partial => 'contests/photo_panel', :locals => { :photos => @my_photos, :contest => @contest }%>
 
<% end %>
